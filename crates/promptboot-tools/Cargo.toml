[package]
name = "promptboot-tools"
version = "0.1.0"
edition = "2021"
publish = false
license = "GPL-3.0-or-later"

[dependencies]
flate2 = { version = "=1.1.9", default-features = false, features = ["rust_backend"] }
promptboot-core = { path = "../promptboot-core" }
serde_json = "=1.0.151"
tar = { version = "=0.4.46", default-features = false }
